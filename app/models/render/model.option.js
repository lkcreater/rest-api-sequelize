module.exports = (db, QueryTypes) => {
    const { sequelize } = db;
    const option = {};

    //====================================================================/
    // insert multiple cateIds to postId
    //====================================================================/
    
    
    return option;
}